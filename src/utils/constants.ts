/**
 * Application-wide shared constants.
 *
 * Centralising magic values here ensures a single place to update them and
 * makes intent clear at every usage site.
 */

/** How long (ms) the "Copied!" confirmation shows before reverting. */
export const COPY_FEEDBACK_DURATION_MS = 2000

/** Width of the player-view popup window in pixels. */
export const PLAYER_VIEW_WIDTH = 400

/** Height of the player-view popup window in pixels. */
export const PLAYER_VIEW_HEIGHT = 600
